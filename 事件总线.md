我会把当前 Top-Tier Event System v3.0 整理成一份可直接照做的 SOP：从目标与边界→事件ID/类型契约→队列与调度→订阅表冻结/COW→Flight Recorder→运维增强→验收与测试清单，按步骤给出交付物与检查点。



# Top-Tier Event System v3.0（Hybrid + Anti-fragile + Ops）SOP 方案文档（可照做落地）
> 目标：你按步骤做，最终得到一套**可证明确定性**、**可验尸**、**防呆**、**可运维**的事件系统：
>
> + **控制面**：EventBus（H/M/L 优先级队列 + BusTask 路由 + 过滤 + 只读订阅表/COW + Flight Recorder）
> + **数据面**：Fast Path（SPSC/RB 直连 IPC，单一所有权）
> + **运维增强**：Promiscuous Hook、Storm Protection、Smart Drop、Budget Police（均不破坏确定性）
>

---

## 0) 前置约束与定义（必须写进文档首页）
### 0.1 系统边界（强制）
+ **控制面（Control Plane）**：低频、逻辑驱动、需要审计/追踪的事件。**允许 N:many**。
+ **数据面（Data Plane）**：高频/硬实时/吞吐敏感数据。**禁止走 EventBus**。走 **SPSC/RB 直连**。

### 0.2 “顶级”判定标准（验收用）
+ 分发路径 **读路径无锁**（Static Freeze 或 COW 读 wait-free）
+ BusTask **只路由，不执行业务**
+ **H 队列延迟上界可解释**（优先级+配额+保留策略）
+ 崩溃后能输出 **死前 N 条控制面事件链**（Flight Recorder）
+ 类型误用在 Debug 可拦截（Type Fingerprint + Typed API）
+ 运维特性全部 **不阻塞、不反压总线**（Hook/告警/输出走旁路）

---

## 1) 产出物清单（每一步都要有证据）
### 1.1 代码/配置交付物（建议路径）
```plain
components/eventbus/
  doc/EventSystem_SOP.md            # 本 SOP 的落地版（你最终提交）
  include/eb_config.h
  include/eb_types.h
  include/eb_api.h
  include/eb_port.h
  src/eb_core.c
  src/eb_subtable.c                # static freeze / COW 二选一实现
  src/eb_trace.c
  src/eb_ops.c                     # hook/storm/drop/budget
platform/ports/<mcu_or_os>/
  eb_port_impl.c                   # timestamp/resetreason/critical/barrier/panic
  linker_noinit.ld / scatter.sct    # .noinit 段（按平台）
tests/eventbus/
  test_matrix.md                   # 验收/压测/故障注入记录
```

### 1.2 文档交付物（必须）
+ `Event ID 分配表`（模块ID/事件ID/优先级/语义/投递/过滤/丢弃/节流策略）
+ `Payload 合同表`（Value/StaticPtr/Handle+SingleOwner/Refcount）
+ `实时性预算表`（H/M/L 处理预算、BusTask 最大循环时间 KPI）
+ `故障注入与复盘报告`（HardFault/WDG/队列满/风暴）

---

## 2) Step-by-Step：架构落地流程（照做）
---

## Step 1：事件分类与事件字典（先做表，再写代码）
### 1.1 建立“事件定义表”（强制字段）
为每个 Event ID 填下列字段（这是后续策略的唯一来源）：

+ `event_id`：32-bit，建议 `module_id<<16 | local_id`
+ `plane`：Control / Data（Data 必须=禁止走 EventBus）
+ `prio`：H/M/L（Control only）
+ `semantic`：Edge / Snapshot
    - **Edge**：不可覆盖、不可合并、不可节流（默认）
    - **Snapshot**：允许覆盖/合并/限频（默认）
+ `delivery`：MAILBOX / SIGNAL（默认）
    - 禁止 DIRECT_CALL（除非写“特许原因+证据链”）
+ `filter_key`：key 的含义（例如 tick 值、from/to、设备ID等）
+ `drop_policy`：DROP_NEW / OVERWRITE / COALESCE_LATEST（仅 Snapshot 可选后两者）
+ `storm_policy`：NONE / MIN_INTERVAL / TOKEN_BUCKET（附参数）
+ `payload_contract`：A/B/C（见 Step 4）

**检查点（必须通过）**

+ 所有 Data 事件在表里标记 **禁止进入总线**
+ 所有 H 事件都有“为何必须 H”的理由（安全/硬实时）
+ 所有 Snapshot 事件明确允许 overwrite/合并，否则默认 Edge

---

## Step 2：总线结构设计（Control Plane）
### 2.1 输入队列：H/M/L 三队列
+ **最低要求**：三队列物理隔离（不能单 FIFO 再打 tag）
+ **调度**：严格优先级 + L 配额（每轮最多处理 L 的 N 条）

### 2.2 BusTask 职责边界（写成强制条款）
BusTask 只允许做：

1. 出队
2. 查订阅表
3. 前置过滤（mask/value on key）
4. 投递到订阅者 mailbox/signal
5. 写 trace/计数器（O(1)）

BusTask 禁止：

+ 任何 I/O（UART/USB/Flash）
+ 字符串格式化
+ 动态内存分配
+ 执行业务回调（默认禁用 direct call）

**检查点**

+ 任何违反项必须在 code review 中 blocker

---

## Step 3：订阅表并发策略（确定性核心）
你必须二选一，写入 `eb_config`，并在文档里声明理由。

### 3.1 策略 A（推荐）：Static Phase + Freeze
+ 系统启动阶段允许 subscribe
+ `main` 进入正常调度前调用 `freeze()`
+ 运行期订阅表只读，分发读路径无锁

**适用**：绝大多数量产系统（确定性最强）

### 3.2 策略 B：COW（Copy-On-Write）订阅表
+ 更新线程构建新数组 → 原子替换指针 → 旧表延迟回收（安全点回收）
+ 分发线程只读，无锁 wait-free

**适用**：确实需要运行时动态模块/插件

**检查点**

+ 分发线程读路径不可拿 mutex
+ 更新路径不可阻塞分发线程（不允许锁住全局表）

---

## Step 4：Payload 合同（消灭指针所有权地狱）
把 payload 规则写死成合同（违者禁止合入）：

### 4.1 合同 A：Control Plane 广播默认 **按值/静态只读指针**
+ payload 只允许：`u32/u64/enum/bitfield` 或 `const static ptr`
+ 禁止动态分配对象指针广播

### 4.2 合同 B：Data Plane 直连允许 **对象池句柄/指针 + 单一所有权**
+ 通过 SPSC/RB 传递
+ 消费者释放（单消费者），无 refcount

### 4.3 合同 C：确需 1:N 零拷贝 → 对象池 + 引用计数
+ 必须显式启用并在事件定义表标注
+ 必须有泄漏检测与超时策略（否则量产不可控）

**检查点**

+ 任何 Control Plane 的动态指针广播必须被拒绝（除非合同 C）

---

## Step 5：类型安全（防呆）
### 5.1 Typed API（默认路径）
+ 提供“类型化 publish/subscribe”用法，让正常使用不需要手写强转

### 5.2 Debug Type Fingerprint（运行时拦截）
+ Event 携带 `type_tag`
+ Subscription 存 `expected_tag`
+ 分发时校验：
    - 不匹配 → 记录 trace + drop / panic（由策略决定）

**检查点**

+ Debug 构建必须能抓到“同尺寸不同类型”的误用
+ Release 构建类型检查编译裁剪为 0 成本

---

## Step 6：前置过滤（消灭惊群）
### 6.1 Filter 的设计原则
+ 过滤只基于 `event.key`，O(1) 判断（mask/value）
+ **过滤必须发生在唤醒订阅者之前**

### 6.2 使用准则（写入事件字典）
+ tick 类：key=tick，订阅者配置 mask/value 或“倍频条件编码进 key”
+ 状态机：key=(from<<16)|to
+ 设备域：key=device_id

**检查点**

+ 订阅者不允许在自己任务里做“无效唤醒后再判断”的模式（否则违背目的）

---

## Step 7：Flight Recorder（死后验尸）
### 7.1 .noinit 段规划
+ Trace buffer + header 放入 `.noinit`
+ header 最少字段：`magic/version/reset_reason/write_idx/wrap_cnt`

### 7.2 启动判定流程
+ 读取 reset_reason：
    - 冷启动：清空 trace
    - 热启动（WDG/HardFault）：保留 trace，提供转存/上传入口

### 7.3 Trace 内容（最小必需）
+ `event_id/prio/key/ts/trace_id/drop_reason(optional)/source(optional)`

**检查点**

+ 触发 WDG/HardFault 后，下一次启动能读取到“死前 N 条事件”

---

## Step 8：Ops 增强（锦上添花，但不得破坏确定性）
这四条都可加，但必须按“硬约束版”实现。

### 8.1 Promiscuous Hook（实时听诊）
+ Hook 仅允许 **O(1) 写入监控缓冲/队列**
+ 禁止 I/O/格式化/阻塞
+ 异步输出由低优先级任务完成
+ 默认关闭，需权限/编译开关/CLI 开启

### 8.2 Storm Protection（风暴防护）
+ 限频粒度：至少 `(event_id, source_id)`
+ 策略：Token Bucket 优先；MIN_INTERVAL 仅用于简单场景
+ H 边沿事件默认不可节流（除非事件定义明确允许）
+ 节流命中只增计数/trace，不发布“节流事件”

### 8.3 Smart Drop（智能丢弃）
+ Edge：禁止 overwrite/coalesce
+ Snapshot：允许 overwrite/coalesce 保鲜
+ 永不阻塞（禁止 BLOCK）
+ 记录 drop reason（满丢/覆盖/节流）

### 8.4 Budget Police（性能守门员）
+ BusTask 记录分段耗时与直方图（p50/p95/p99）
+ 超预算告警 **不走总线**，写入 trace/独立性能通道
+ 告警限频，防自激

**检查点**

+ Ops 功能在最坏情况下不会让 BusTask 变慢或阻塞（强制）

---

## Step 9：验收与压测（必须做，不做等于没落地）
### 9.1 功能验收
+ 订阅/发布/过滤/优先级调度符合事件定义表
+ Freeze/COW 行为符合配置

### 9.2 实时性验收（关键）
+ 构造：L 洪水（UI/log 1000 条）+ 插入 H 急停  
**期望**：H 在上界内被处理（给出测量数据）
+ 构造：订阅者数量拉满（例如 50）  
**期望**：Budget Police 记录可解释的 p95/p99

### 9.3 故障注入
+ WDG reset、HardFault、队列满、Hook 输出拥塞、风暴事件
+ **期望**：Flight Recorder 留证据；总线不被 Hook/告警反压

### 9.4 交付证据
+ `tests/eventbus/test_matrix.md`：每项测试的步骤、结果、截图/日志路径

---

# 3) 你写 SOP 文档时的“推荐章节模板”（直接照抄结构）
1. 背景与目标
2. 控制面/数据面边界与禁止项
3. 事件字典与策略表（Event Definition Table）
4. Control Plane：H/M/L 队列与 BusTask 调度策略
5. 订阅表并发策略：Freeze / COW（二选一）
6. Payload 合同：A/B/C 与禁止项
7. 类型安全：Typed API + Type Fingerprint（Debug）
8. 前置过滤：key 语义与 mask/value 使用规范
9. Flight Recorder：.noinit、启动判定、转存策略
10. Operational Excellence：Hook/Storm/Drop/Budget（硬约束版）
11. 验收与测试矩阵（含 KPI 指标）
12. 风险清单与回滚策略（可选）

---

## 4) 你下一步怎么做（最短落地路线）
按顺序完成并提交“可审计证据”：

1. **先填事件定义表**（至少覆盖现有模块的 20–50 个事件）
2. 选订阅表策略：**Freeze（推荐）或 COW**
3. 明确 payload 合同（Control 面一律按值/静态只读）
4. 写测试矩阵（先写再实现，避免遗漏）

你把“事件定义表（哪怕是草表）”贴出来，我可以按上述 SOP 直接帮你做一次**架构评审式红线检查**（哪里不符合 Top-Tier 约束，逐条给修改意见）。

